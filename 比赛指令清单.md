# racecar 比赛指令清单

下面指令按比赛实际使用顺序排列。默认工作空间路径为：

```bash
/home/davinci-mini/CHEN2
```

如果你的车上路径不同，把命令里的路径改成你的实际路径。

## 0. 每个终端都先执行

```bash
cd CHEN2
source install/setup.bash
```

如果刚改过代码，需要先编译：

```bash
cd /home/davinci-mini/CHEN2
colcon build --symlink-install
source install/setup.bash
```

## 推荐：使用一键 Python 菜单脚本

建议比赛时优先使用这个脚本，不用反复新建终端、一条一条输入命令。

第一次使用或代码改动后先编译：

```bash
cd /home/davinci-mini/CHEN2
chmod +x src/racecar/scripts/racecar_helper.py
colcon build --symlink-install --packages-select racecar
source install/setup.bash
```

启动一键菜单：

```bash
cd /home/davinci-mini/CHEN2
source install/setup.bash
ros2 run racecar racecar_helper.py
```

菜单常用顺序：

```text
1  启动小车底盘、雷达、IMU、EKF
2  启动 GMapping 建图
3  启动键盘控制，跑完一圈后按 Ctrl+C 回到菜单
4  保存地图到 /home/davinci-mini/CHEN2/src/racecar/maps/race_map
15 停止建图和键盘控制
5  启动正式比赛导航，自动使用 race_map.yaml
6  打开 RViz
7  选择导航点模式
8  选择停车点模式
9  确认点位
10 ARM 比赛
```

说明：菜单 1 的 `Run_car.launch.py` 输出会写入日志，避免“文件溢出”刷屏影响操作。日志位置：

```bash
/home/davinci-mini/CHEN2/logs/
```

如果 `ros2 run racecar racecar_helper.py` 提示 `No executable found`，重新执行：

```bash
cd /home/davinci-mini/CHEN2
chmod +x src/racecar/scripts/racecar_helper.py
colcon build --symlink-install --packages-select racecar
source install/setup.bash
```

下面第 1 步开始是手动分终端执行方式，作为备用。

## 1. 启动小车底盘、雷达、IMU、编码器、EKF

新开终端 1：

```bash
cd /home/davinci-mini/CHEN2
source install/setup.bash
ros2 launch racecar Run_car.launch.py
```

检查雷达、里程计是否有数据：

```bash
ros2 topic echo /scan --once
ros2 topic echo /odom_combined --once
```

## 2. 建图阶段：启动 GMapping SLAM

新开终端 2：

```bash
cd /home/davinci-mini/CHEN2
source install/setup.bash
ros2 launch slam_gmapping slam_gmapping.launch.py
```

如果要打开 RViz 查看建图效果：

```bash
rviz2
```

RViz 中建议添加：

- `Map`，话题选 `/map`
- `LaserScan`，话题选 `/scan`
- `TF`

## 3. 建图阶段：键盘控制小车跑一圈

新开终端 3：

```bash
cd /home/davinci-mini/CHEN2
source install/setup.bash
ros2 run racecar racecar_teleop.py
```

键盘控制说明：

```text
i    前进
o/u  前进右转 / 前进左转
j/l  原地左转 / 原地右转
m/,/. 后退左转 / 后退 / 后退右转
space 或 k 停车
w/s  调整速度偏置
a/d  调整转向偏置
```

建图时要让挡板、锥桶都被雷达扫到，地图中显示出来。

## 4. 保存地图

跑完一圈后，新开终端 4 保存地图：

```bash
cd /home/davinci-mini/CHEN2
source install/setup.bash
ros2 run nav2_map_server map_saver_cli -f /home/davinci-mini/CHEN2/src/racecar/maps/race_map
```

保存后应该生成：

```bash
ls /home/davinci-mini/CHEN2/src/racecar/maps/race_map.yaml
ls /home/davinci-mini/CHEN2/src/racecar/maps/race_map.pgm
```

如果保存地图后还要重新编译安装地图资源：

```bash
cd /home/davinci-mini/CHEN2
colcon build --symlink-install
source install/setup.bash
```

## 5. 人工把小车开回起点

继续使用键盘控制终端，把小车开回比赛起点并停好：

```text
space 或 k 停车
```

确认小车在起点附近后，结束建图相关终端：

- 可以关闭 `slam_gmapping` 终端
- 可以关闭 `racecar_teleop.py` 终端
- `Run_car.launch.py` 可以继续保留运行

## 6. 启动正式比赛导航

新开终端 2 或重新使用一个终端：

```bash
cd /home/davinci-mini/CHEN2
source install/setup.bash
ros2 launch racecar Run_race.launch.py map:=/home/davinci-mini/CHEN2/src/racecar/maps/race_map.yaml
```

如果想使用默认旧地图测试，也可以：

```bash
ros2 launch racecar Run_race.launch.py
```

但正式比赛建议显式传入刚保存的地图。

## 7. RViz 中显示地图、路径点和停车点

新开终端：

```bash
rviz2
```

RViz 中建议添加：

- `Map`，话题选 `/map`
- `TF`
- `LaserScan`，话题选 `/scan`
- `Path`，话题选 `/plan`
- `MarkerArray`，话题选 `/race/preview_markers`
- `PoseArray` 或 AMCL 粒子显示，根据 RViz 中可用话题选择

## 8. 在 RViz 地图上点多点导航目标

`race_manager` 默认就是导航点采集模式。

如果你想明确切换到导航点采集模式：

```bash
ros2 service call /race/select_waypoints std_srvs/srv/Trigger {}
```

然后在 RViz 工具栏选择：

```text
Publish Point
```

在地图上按顺序点击导航点：

```text
P1 -> P2 -> P3 -> ... -> 终点
```

RViz 中 `/race/preview_markers` 会显示绿色箭头和编号。

## 9. 在 RViz 地图上点两个停车点

切换到停车点采集模式：

```bash
ros2 service call /race/select_parking std_srvs/srv/Trigger {}
```

继续用 RViz 的 `Publish Point` 点击两个停车点：

```text
stop_1
stop_2
```

RViz 中 `/race/preview_markers` 会显示黄色圆形区域和停车时间。

停车时间在这里改：

```bash
nano /home/davinci-mini/CHEN2/src/racecar/config/race_params.yaml
```

主要参数：

```yaml
parking_dwell_seconds: [3.0, 5.0]
parking_point_match_radius: 0.8
max_linear_speed: 0.35
max_angular_speed: 0.8
```

修改后重新启动 `Run_race.launch.py`。

## 10. 确认点位

导航点和两个停车点都显示正确后执行：

```bash
ros2 service call /race/confirm_points std_srvs/srv/Trigger {}
```

成功后会提示点位已确认。

如果点错了，清空后重新点：

```bash
ros2 service call /race/clear_points std_srvs/srv/Trigger {}
ros2 service call /race/select_waypoints std_srvs/srv/Trigger {}
```

然后重新执行第 8、9、10 步。

## 11. ARM 比赛，等待离开起点后计时

确认小车已经停在起点附近，然后执行：

```bash
ros2 service call /race/arm std_srvs/srv/Trigger {}
```

此时不会立刻计时。

当小车再次离开起点半径后，程序才会：

```text
开始计时
发送第一个导航点
开始 /cmd_vel -> /car_cmd_vel 速度转发
自动导航
到停车点停车
完成后输出总用时
```

## 12. 比赛中常用查看命令

查看状态日志：

```bash
ros2 node list
```

查看速度输出：

```bash
ros2 topic echo /cmd_vel
ros2 topic echo /car_cmd_vel
```

查看里程计：

```bash
ros2 topic echo /odom_combined
```

查看地图：

```bash
ros2 topic echo /map --once
```

查看服务：

```bash
ros2 service list | grep /race
```

查看 Nav2 action：

```bash
ros2 action list
```

## 13. 暂停、取消、重置

取消比赛并停车：

```bash
ros2 service call /race/cancel std_srvs/srv/Trigger {}
```

重置比赛状态，保留已经确认的点位：

```bash
ros2 service call /race/reset std_srvs/srv/Trigger {}
```

重新 arm：

```bash
ros2 service call /race/arm std_srvs/srv/Trigger {}
```

清空所有点位，重新在 RViz 点：

```bash
ros2 service call /race/clear_points std_srvs/srv/Trigger {}
```

## 14. 雷达开关命令

打开雷达：

```bash
ros2 topic pub -1 /lslidar_order std_msgs/msg/Int8 "{data: 1}"
```

关闭雷达：

```bash
ros2 topic pub -1 /lslidar_order std_msgs/msg/Int8 "{data: 0}"
```

## 15. 比赛参数调节

打开参数文件：

```bash
nano /home/davinci-mini/CHEN2/src/racecar/config/race_params.yaml
```

常调参数：

```yaml
max_linear_speed: 0.35
max_angular_speed: 0.8
parking_dwell_seconds: [3.0, 5.0]
parking_point_match_radius: 0.8
start_detect_radius: 0.30
start_leave_radius: 0.45
```

Nav2 避障参数文件：

```bash
nano /home/davinci-mini/CHEN2/src/racecar/config/nav.yaml
```

常调参数：

```yaml
inflation_radius: 0.35
cost_scaling_factor: 8.0
use_collision_detection: true
desired_linear_vel: 0.33
minimum_turning_radius: 0.6
```

修改参数后重新编译并重新启动对应 launch：

```bash
cd /home/davinci-mini/CHEN2
colcon build --symlink-install
source install/setup.bash
```

## 16. 完整比赛顺序简表

```bash
# 终端 1：启动小车
cd /home/davinci-mini/CHEN2
source install/setup.bash
ros2 launch racecar Run_car.launch.py

# 终端 2：建图
cd /home/davinci-mini/CHEN2
source install/setup.bash
ros2 launch slam_gmapping slam_gmapping.launch.py

# 终端 3：键盘跑一圈
cd /home/davinci-mini/CHEN2
source install/setup.bash
ros2 run racecar racecar_teleop.py

# 终端 4：保存地图
cd /home/davinci-mini/CHEN2
source install/setup.bash
ros2 run nav2_map_server map_saver_cli -f /home/davinci-mini/CHEN2/src/racecar/maps/race_map

# 人工开回起点，关闭建图和键盘终端

# 终端 2：启动正式比赛导航
cd /home/davinci-mini/CHEN2
source install/setup.bash
ros2 launch racecar Run_race.launch.py map:=/home/davinci-mini/CHEN2/src/racecar/maps/race_map.yaml

# RViz：显示 /map、/plan、/race/preview_markers
rviz2

# 点导航点
ros2 service call /race/select_waypoints std_srvs/srv/Trigger {}

# 点停车点
ros2 service call /race/select_parking std_srvs/srv/Trigger {}

# 确认点位
ros2 service call /race/confirm_points std_srvs/srv/Trigger {}

# 小车在起点后 arm
ros2 service call /race/arm std_srvs/srv/Trigger {}
```
